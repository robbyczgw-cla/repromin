# Copy to .env locally. Never commit .env.
# ReproMin v0.1 does not need an API key. These are placeholders
# so a later public clone does not invent a secrets file.

# REPROMIN_PORT=7878
# PLAYWRIGHT_BROWSERS_PATH=
